# CHANGELOG.MD - Журнал разработки
v0.9.6-dev - 2025-09-27
### Added
- Одноразовая коррекция шагов при срабатывании микрика в районе 59:58
- Логика расчёта дельты по времени и пересчёт шагов
- Логирование коррекции: дельта в секундах и шаги

### Removed
- Удалена поддержка дробного хода стрелки (`stepFraction`)
- Удалена функция `arrowInitParams()`

### Changed
- Упрощён FSM: теперь шаги всегда равны `StepsForMinute`, кроме случая коррекции
- Логика предварительного старта теперь учитывает возможную коррекцию

v0.9.6-dev — «Глава о стрелке и её судьбе»
Конечный автомат Мы долго спорили, как должна вести себя стрелка, когда её путь прерывает концевик, и наконец пришли к согласию: переход из MOVING в IDLE теперь сопровождается остановкой и снятием питания, словно герой, вернувшийся домой и снявший сапоги у порога.

SET_STATE Макрос уступил место функции, и с этим изменением логика обрела ясность и достоинство. Теперь смена состояний — не хаотичный крик, а размеренный шаг, записанный в летопись.

Параметры движения Скорость возросла до 750 шагов в секунду, ускорение — до 500. Стрелка идёт быстрее, но не теряет плавности, как человек, который научился спешить без суеты.

Инициализация Время перехода теперь выводится из одного эталонного числа — baseTransitionSec. Инженеру больше не нужно гадать: всё пересчитывается пропорционально, как в жизни, где каждое мгновение связано с другим.

Логика микрика Сторожевой пес механизма — микропереключатель — теперь отзывается только на верные сигналы, игнорируя ложные тревоги.

## 📝 Changelog — v0.9.5-dev  
**Дата:** __.09.2025  
**Статус:** dev (тестовая сборка)
✨ Added
Обслуживание системы на 15‑й минуте
Перенос функции синхронизации RTC из wifi.cpp в debug.cpp.
Синхронизация выполняется раз в час при arrowState == IDLE.

Мониторинг батареи
Замер напряжения VBAT через АЦП.
Логирование значения раз в час (на 01‑й минуте), только если FSM в состоянии IDLE.

🔧 Changed
Централизация сервисных функций в debug.cpp для удобства сопровождения.
FSM теперь не перегружается диагностикой в моменты движения стрелки или боя.

## 📝 Changelog — v0.9.4-dev  
**Дата:** 16.09.2025  
**Статус:** dev (тестовая сборка)

Added
Предварительный старт (pre‑start) FSM
Введено промежуточное состояние перед движением стрелки для компенсации люфтов.
FSM теперь проходит цепочку IDLE → LAG → MOVING → IDLE.
Лог переходов фиксируется через SET_STATE.

🔧 Changed
Плавное движение стрелки
Вместо одного рывка реализовано пошаговое движение с параметрами «шаг» и «интервал».
Параметры вынесены в конфиг для удобной настройки.
FSM в состоянии MOVING теперь обрабатывает серию шагов до целевой позиции.

🐞 Fixed
Устранены дублирующиеся события при смене минут.
lastRtcMinute синхронизируется при первом запуске, предотвращая ложный старт.

🐝 Debug/Logging
Логи теперь показывают все переходы FSM, включая промежуточный LAG.
Добавлен вывод времени и состояния для отладки плавного движения.

## 📝 Changelog — v0.9.3-dev  
**Дата:** 12.09.2025  
**Статус:** dev (тестовая сборка)

### ✨ Новое
- Универсальный логгер:
  - `debugLog()` для готовых строк.
  - `debugLogf()` для форматированных сообщений (аналог printf).
  - Логи теперь автоматически уходят и в Serial, и в веб‑панель.
- Веб‑монитор теперь показывает:
  - Переходы FSM.
  - События после синхронизации RTC с NTP.
  - Полный ход работы без необходимости вручную вызывать `debugDump()`.
  - Реализован «живой» накопительный лог прямо в браузере, пока вкладка открыта.

### 🔧 Изменения
- Логгер интегрирован в FSM и ключевые события старта.
- Логические блоки подключения к Wi-Fi оставлены только в Serial, чтобы веб‑лог начинался с момента доступности сети.
- Мелкие правки HTML/CSS веб‑панели.
- Вывод версии проекта в веб‑монитор.
- Настройка размеров окна лога и частоты обновления.

## 📝 Changelog — v0.9.1-dev  
**Дата:** 11.09.2025  
**Статус:** dev (тестовая сборка)

### ✨ Новое
- **Веб-монитор в `debug.cpp`**  
  - Отдаёт `/status` (RTC, uptime, StepsForMinute, FSM).  
  - Отдаёт `/log` с последними 50 строками лога (те же, что выводятся в Serial по команде `d`).  
  - HTML-панель с автообновлением каждые 2 сек и возможностью менять `StepsForMinute` прямо из браузера.  
  - Порт по умолчанию — **80** (настраивается в `config.h`).

### 🔧 Изменения
- Параметры Wi-Fi (`WIFI_SSID`, `WIFI_PASSWORD`, `WEB_PORT`) вынесены в `config.h`.
- Увеличено время ожидания подключения к Wi-Fi с ~11 до ~20 секунд.
- Добавлены:
  - `WiFi.mode(WIFI_STA)`
  - `WiFi.setAutoReconnect(true)`
  - `WiFi.persistent(true)`  
  для стабильного подключения и автопереподключения.
- Удалены временные заглушки и дубли функций (`stateName`).

### 🐞 Исправлено
- Конфликты линковки из-за дублирующегося `stateName()`.
- Ошибка `undefined reference to rtcNow` — теперь время берётся напрямую из `rtc.now()`.

### 📌 План на v0.9.2-dev
- Запуск веб-монитора **только после** успешного подключения к Wi-Fi.
- Вывод версии проекта в веб-монитор.
- Возможность SSE-логов в реальном времени.
- Мини-авторизация для POST-запросов.

## [0.9.0-dev] - 2025-09-10
### Added
- Поддержка «получасового BREAK» — ожидание до 30-й минуты при раннем срабатывании второго кулачка (27–29 мин).
- Логика выхода из BREAK теперь универсальна (00 или 30 минута).
- Расширенные условия нормального срабатывания микрика (27–29 мин вместо только 29).

### Changed
- Рефакторинг FSM стрелки: упрощена структура `case` и условия переходов.
- Упорядочены `#include` в модулях, устранены конфликты имён с библиотекой `<WiFi.h>`.
- Перенос функций по файлам для лучшей модульности.

### Fixed
- Исправлен фальстарт при переходе из BREAK на 59-й минуте.
- Устранена рекурсивная петля `#include` и дублирование определений функций.
- Очистка кэша Arduino IDE для устранения «призраков» старых `.o` файлов.

--
